# 🔥 Firebase Setup for Residex - Step-by-Step Guide

## 📋 Overview

You're absolutely right! Let's build the **data foundation first** before integrating AI. Here's the systematic approach:

---

## 🎯 Phase 1: Firebase Project Setup (30 minutes)

### Step 1: Create Firebase Project

1. **Go to Firebase Console**
   - Visit: https://console.firebase.google.com/
   - Click "Add project"

2. **Project Configuration**
   ```
   Project name: Residex
   Analytics: Enable (for tracking user behavior)
   Analytics location: Malaysia
   ```

3. **Enable Google Analytics**
   - Create new Analytics account: "Residex Analytics"
   - Accept terms

---

### Step 2: Add Flutter Apps to Firebase

#### **2A: Android App**

1. In Firebase Console → Project Overview → Add app → Android
2. **Register Android App:**
   ```
   Android package name: com.residex.app
   App nickname: Residex Android
   Debug signing certificate (optional for now)
   ```

3. **Download `google-services.json`**
   - Save to: `Residex_app/android/app/google-services.json`

4. **Update Android Configuration**

````gradle
buildscript {
    dependencies {
        classpath("com.google.gms:google-services:4.4.0") // Add this
    }
}
````

````gradle
plugins {
    id("com.android.application")
    id("kotlin-android")
    id("dev.flutter.flutter-gradle-plugin")
    id("com.google.gms.google-services") // Add this
}

dependencies {
    implementation(platform("com.google.firebase:firebase-bom:32.7.0"))
    implementation("com.google.firebase:firebase-analytics")
    implementation("com.google.firebase:firebase-auth")
    implementation("com.google.firebase:firebase-firestore")
    implementation("com.google.firebase:firebase-storage")
}
````

---

#### **2B: iOS App (Optional for hackathon)**

1. Firebase Console → Add app → iOS
2. **Register iOS App:**
   ```
   iOS bundle ID: com.residex.app
   App nickname: Residex iOS
   ```

3. **Download `GoogleService-Info.plist`**
   - Save to: `Residex_app/ios/Runner/GoogleService-Info.plist`

---

### Step 3: Add Firebase Dependencies to Flutter

````yaml
dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9

  # Firebase Core
  firebase_core: ^2.24.2
  
  # Firebase Services
  firebase_auth: ^4.16.0
  firebase_firestore: ^4.14.0
  firebase_storage: ^11.6.0
  cloud_firestore: ^4.14.0
  
  # Google Sign In
  google_sign_in: ^6.2.1
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Existing dependencies...
  google_fonts: ^6.1.0
  # ... rest of your dependencies ...
````

Run:
```bash
cd Residex_app
flutter pub get
```

---

### Step 4: Initialize Firebase in Flutter

````dart
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:firebase_core/firebase_core.dart';
import 'firebase_options.dart'; // Generated file
import 'core/theme/app_theme.dart';
import 'core/router/app_router.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();

  // Initialize Firebase
  await Firebase.initializeApp(
    options: DefaultFirebaseOptions.currentPlatform,
  );

  SystemChrome.setSystemUIOverlayStyle(
    const SystemUiOverlayStyle(
      statusBarColor: Colors.transparent,
      statusBarIconBrightness: Brightness.light,
      systemNavigationBarColor: AppColors.background,
      systemNavigationBarIconBrightness: Brightness.light,
    ),
  );

  runApp(
    const ProviderScope(
      child: ResidexApp(),
    ),
  );
}

class ResidexApp extends StatelessWidget {
  const ResidexApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Residex',
      debugShowCheckedModeBanner: false,
      theme: AppTheme.darkTheme,
      initialRoute: AppRouter.splash, // Change to splash for auth check
      onGenerateRoute: AppRouter.generateRoute,
    );
  }
}
````

---

### Step 5: Generate Firebase Options

Run FlutterFire CLI to auto-generate platform-specific config:

```bash
# Install FlutterFire CLI
dart pub global activate flutterfire_cli

# Generate firebase_options.dart
flutterfire configure
```

This creates:
```
Residex_app/lib/firebase_options.dart
```

Select:
- ✅ Android
- ✅ iOS
- ⬜ Web (skip for now)
- ⬜ macOS (skip)
- ⬜ Windows (skip)

---

## 🗄️ Phase 2: Firestore Database Structure

### Step 1: Enable Firestore

1. Firebase Console → Build → Firestore Database
2. Click "Create database"
3. **Security Mode:**
   ```
   Start in test mode (for development)
   ⚠️ Change before production!
   ```

4. **Location:**
   ```
   asia-southeast1 (Singapore) - Closest to Malaysia
   ```

---

### Step 2: Define Core Collections

Create this structure in Firestore:

```
firestore/
├── users/                          # User profiles
│   └── {userId}/
│       ├── role: 'landlord' | 'tenant'
│       ├── email: string
│       ├── displayName: string
│       ├── phoneNumber: string
│       ├── photoURL: string
│       ├── createdAt: timestamp
│       └── updatedAt: timestamp
│
├── properties/                     # Landlord properties
│   └── {propertyId}/
│       ├── landlordId: string (ref to users)
│       ├── name: string
│       ├── address: string
│       ├── type: 'apartment' | 'house' | 'condo'
│       ├── totalUnits: number
│       ├── monthlyRent: number
│       ├── status: 'active' | 'inactive'
│       ├── images: string[]
│       ├── amenities: string[]
│       ├── createdAt: timestamp
│       └── units/                  # Subcollection
│           └── {unitId}/
│               ├── unitNumber: string
│               ├── status: 'occupied' | 'vacant'
│               ├── monthlyRent: number
│               └── currentTenantId: string (nullable)
│
├── leases/                         # Rental agreements
│   └── {leaseId}/
│       ├── landlordId: string
│       ├── tenantId: string
│       ├── propertyId: string
│       ├── unitId: string
│       ├── startDate: timestamp
│       ├── endDate: timestamp
│       ├── monthlyRent: number
│       ├── securityDeposit: number
│       ├── status: 'active' | 'expired' | 'terminated'
│       ├── contractPdfUrl: string (Firebase Storage link)
│       ├── specialTerms: map
│       ├── createdAt: timestamp
│       └── updatedAt: timestamp
│
├── maintenanceTickets/             # Repair requests
│   └── {ticketId}/
│       ├── tenantId: string
│       ├── landlordId: string
│       ├── propertyId: string
│       ├── unitId: string
│       ├── title: string
│       ├── description: string
│       ├── category: 'plumbing' | 'electrical' | 'ac'
│       ├── urgency: 'low' | 'medium' | 'high' | 'urgent'
│       ├── status: 'open' | 'acknowledged' | 'in_progress' | 'resolved'
│       ├── imageUrls: string[]
│       ├── estimatedCost: number (nullable)
│       ├── slaDeadline: timestamp
│       ├── createdAt: timestamp
│       ├── updatedAt: timestamp
│       └── timeline/               # Subcollection
│           └── {eventId}/
│               ├── action: string ('created', 'acknowledged', 'escalated')
│               ├── actor: string (userId)
│               ├── details: string
│               └── timestamp: timestamp
│
├── bills/                          # Utility bills & rent
│   └── {billId}/
│       ├── landlordId: string
│       ├── tenantId: string (nullable for property-level bills)
│       ├── propertyId: string
│       ├── unitId: string (nullable)
│       ├── type: 'rent' | 'electricity' | 'water' | 'internet'
│       ├── amount: number
│       ├── dueDate: timestamp
│       ├── paidDate: timestamp (nullable)
│       ├── status: 'pending' | 'paid' | 'overdue'
│       ├── billImageUrl: string
│       ├── ocrData: map (extracted text)
│       ├── createdAt: timestamp
│       └── updatedAt: timestamp
│
└── documents/                      # RAG document storage (DocuMind)
    └── {documentId}/
        ├── uploadedBy: string (userId)
        ├── propertyId: string
        ├── category: 'lease' | 'regulation' | 'warranty' | 'receipt'
        ├── fileName: string
        ├── fileUrl: string (Firebase Storage)
        ├── fileType: 'pdf' | 'jpg' | 'png'
        ├── fileSize: number
        ├── ocrText: string (for RAG indexing)
        ├── metadata: map
        ├── createdAt: timestamp
        └── tags: string[]
```

---

### Step 3: Set Up Firestore Security Rules

````javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }
    
    function isLandlord() {
      return isAuthenticated() && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'landlord';
    }
    
    function isTenant() {
      return isAuthenticated() && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'tenant';
    }
    
    // Users collection
    match /users/{userId} {
      allow read: if isAuthenticated();
      allow create: if isOwner(userId);
      allow update: if isOwner(userId);
      allow delete: if isOwner(userId);
    }
    
    // Properties collection
    match /properties/{propertyId} {
      allow read: if isAuthenticated();
      allow create: if isLandlord();
      allow update: if isLandlord() && resource.data.landlordId == request.auth.uid;
      allow delete: if isLandlord() && resource.data.landlordId == request.auth.uid;
      
      // Units subcollection
      match /units/{unitId} {
        allow read: if isAuthenticated();
        allow write: if isLandlord() && 
                       get(/databases/$(database)/documents/properties/$(propertyId)).data.landlordId == request.auth.uid;
      }
    }
    
    // Leases collection
    match /leases/{leaseId} {
      allow read: if isAuthenticated() && 
                    (resource.data.landlordId == request.auth.uid || 
                     resource.data.tenantId == request.auth.uid);
      allow create: if isLandlord();
      allow update: if isLandlord() && resource.data.landlordId == request.auth.uid;
      allow delete: if isLandlord() && resource.data.landlordId == request.auth.uid;
    }
    
    // Maintenance tickets
    match /maintenanceTickets/{ticketId} {
      allow read: if isAuthenticated() && 
                    (resource.data.landlordId == request.auth.uid || 
                     resource.data.tenantId == request.auth.uid);
      allow create: if isTenant();
      allow update: if isAuthenticated() && 
                      (resource.data.landlordId == request.auth.uid || 
                       resource.data.tenantId == request.auth.uid);
      
      // Timeline subcollection
      match /timeline/{eventId} {
        allow read: if isAuthenticated();
        allow create: if isAuthenticated();
      }
    }
    
    // Bills collection
    match /bills/{billId} {
      allow read: if isAuthenticated() && 
                    (resource.data.landlordId == request.auth.uid || 
                     resource.data.tenantId == request.auth.uid);
      allow create: if isLandlord();
      allow update: if isAuthenticated() && 
                      (resource.data.landlordId == request.auth.uid || 
                       resource.data.tenantId == request.auth.uid);
    }
    
    // Documents collection
    match /documents/{documentId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated();
      allow update: if isAuthenticated() && resource.data.uploadedBy == request.auth.uid;
      allow delete: if isAuthenticated() && resource.data.uploadedBy == request.auth.uid;
    }
  }
}
````

---

## 📦 Phase 3: Firebase Storage Setup

### Step 1: Enable Firebase Storage

1. Firebase Console → Build → Storage
2. Click "Get started"
3. **Security Mode:**
   ```
   Start in test mode
   ```

4. **Location:**
   ```
   asia-southeast1 (same as Firestore)
   ```

---

### Step 2: Create Storage Buckets Structure

```
gs://residex.appspot.com/
├── contracts/                      # Lease PDFs
│   └── {leaseId}.pdf
│
├── maintenance/                    # Ticket photos
│   └── {ticketId}/
│       ├── before_1.jpg
│       ├── before_2.jpg
│       └── after_1.jpg
│
├── bills/                          # Bill scans
│   └── {billId}/
│       └── bill_scan.jpg
│
├── documents/                      # DocuMind files
│   └── {documentId}/
│       └── original_file.pdf
│
└── profiles/                       # User avatars
    └── {userId}.jpg
```

---

### Step 3: Set Storage Security Rules

````javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    // Contracts (only landlords can upload, both can read)
    match /contracts/{leaseId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated(); // Landlord creates, tenant reviews
    }
    
    // Maintenance photos (tenant uploads, landlord reads)
    match /maintenance/{ticketId}/{fileName} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated(); // Both parties can upload evidence
    }
    
    // Bills (landlord uploads, tenant reads)
    match /bills/{billId}/{fileName} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }
    
    // Documents (RAG system)
    match /documents/{documentId}/{fileName} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }
    
    // Profile photos
    match /profiles/{userId} {
      allow read: if isAuthenticated();
      allow write: if isOwner(userId);
    }
  }
}
````

---

## 🔐 Phase 4: Firebase Authentication Setup

### Step 1: Enable Firebase Authentication

1. Firebase Console → Build → Authentication
2. Click "Get started"
3. **Enable Sign-in Methods:**

   ✅ **Email/Password**
   - Click "Email/Password" → Enable → Save

   ✅ **Google Sign-In**
   - Click "Google" → Enable
   - Project support email: your@email.com
   - Save

---

### Step 2: Create Authentication Service

````dart
import 'package:firebase_auth/firebase_auth.dart';
import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:google_sign_in/google_sign_in.dart';

class AuthService {
  final FirebaseAuth _auth = FirebaseAuth.instance;
  final FirebaseFirestore _firestore = FirebaseFirestore.instance;
  final GoogleSignIn _googleSignIn = GoogleSignIn();

  // Get current user
  User? get currentUser => _auth.currentUser;

  // Auth state stream
  Stream<User?> get authStateChanges => _auth.authStateChanges();

  // Sign up with email/password
  Future<UserCredential> signUpWithEmail({
    required String email,
    required String password,
    required String displayName,
    required String role, // 'landlord' or 'tenant'
  }) async {
    try {
      // Create auth user
      final userCredential = await _auth.createUserWithEmailAndPassword(
        email: email,
        password: password,
      );

      // Update display name
      await userCredential.user!.updateDisplayName(displayName);

      // Create Firestore user document
      await _firestore.collection('users').doc(userCredential.user!.uid).set({
        'email': email,
        'displayName': displayName,
        'role': role,
        'phoneNumber': null,
        'photoURL': null,
        'createdAt': FieldValue.serverTimestamp(),
        'updatedAt': FieldValue.serverTimestamp(),
      });

      return userCredential;
    } catch (e) {
      throw Exception('Sign up failed: $e');
    }
  }

  // Sign in with email/password
  Future<UserCredential> signInWithEmail({
    required String email,
    required String password,
  }) async {
    try {
      return await _auth.signInWithEmailAndPassword(
        email: email,
        password: password,
      );
    } catch (e) {
      throw Exception('Sign in failed: $e');
    }
  }

  // Sign in with Google
  Future<UserCredential> signInWithGoogle({required String role}) async {
    try {
      // Trigger Google Sign-In flow
      final GoogleSignInAccount? googleUser = await _googleSignIn.signIn();
      if (googleUser == null) throw Exception('Google sign-in cancelled');

      // Get authentication details
      final GoogleSignInAuthentication googleAuth = await googleUser.authentication;

      // Create Firebase credential
      final credential = GoogleAuthProvider.credential(
        accessToken: googleAuth.accessToken,
        idToken: googleAuth.idToken,
      );

      // Sign in to Firebase
      final userCredential = await _auth.signInWithCredential(credential);

      // Check if user exists in Firestore
      final userDoc = await _firestore
          .collection('users')
          .doc(userCredential.user!.uid)
          .get();

      if (!userDoc.exists) {
        // Create new user document
        await _firestore.collection('users').doc(userCredential.user!.uid).set({
          'email': userCredential.user!.email,
          'displayName': userCredential.user!.displayName,
          'role': role,
          'phoneNumber': userCredential.user!.phoneNumber,
          'photoURL': userCredential.user!.photoURL,
          'createdAt': FieldValue.serverTimestamp(),
          'updatedAt': FieldValue.serverTimestamp(),
        });
      }

      return userCredential;
    } catch (e) {
      throw Exception('Google sign-in failed: $e');
    }
  }

  // Sign out
  Future<void> signOut() async {
    await Future.wait([
      _auth.signOut(),
      _googleSignIn.signOut(),
    ]);
  }

  // Get user role from Firestore
  Future<String?> getUserRole(String userId) async {
    try {
      final userDoc = await _firestore.collection('users').doc(userId).get();
      return userDoc.data()?['role'] as String?;
    } catch (e) {
      return null;
    }
  }

  // Reset password
  Future<void> resetPassword(String email) async {
    await _auth.sendPasswordResetEmail(email: email);
  }
}
````

---

### Step 3: Create Auth Provider (Riverpod)

````dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:firebase_auth/firebase_auth.dart';
import '../services/auth_service.dart';

// Auth service provider
final authServiceProvider = Provider<AuthService>((ref) {
  return AuthService();
});

// Current user provider
final currentUserProvider = StreamProvider<User?>((ref) {
  final authService = ref.watch(authServiceProvider);
  return authService.authStateChanges;
});

// User role provider
final userRoleProvider = FutureProvider<String?>((ref) async {
  final authService = ref.watch(authServiceProvider);
  final user = authService.currentUser;
  
  if (user == null) return null;
  
  return await authService.getUserRole(user.uid);
});
````

---

## 🚀 Phase 5: Test Firebase Connection

### Step 1: Create Test Screen

````dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:cloud_firestore/cloud_firestore.dart';
import '../../../../core/theme/app_theme.dart';

class TestFirebaseScreen extends ConsumerWidget {
  const TestFirebaseScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      backgroundColor: AppColors.background,
      appBar: AppBar(
        title: const Text('Firebase Test'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            ElevatedButton(
              onPressed: () async {
                try {
                  // Test Firestore write
                  await FirebaseFirestore.instance
                      .collection('test')
                      .add({
                    'message': 'Hello Residex!',
                    'timestamp': FieldValue.serverTimestamp(),
                  });
                  
                  ScaffoldMessenger.of(context).showSnackBar(
                    const SnackBar(content: Text('✅ Firebase connected!')),
                  );
                } catch (e) {
                  ScaffoldMessenger.of(context).showSnackBar(
                    SnackBar(content: Text('❌ Error: $e')),
                  );
                }
              },
              child: const Text('Test Firestore'),
            ),
            
            const SizedBox(height: 20),
            
            ElevatedButton(
              onPressed: () async {
                try {
                  // Test Firestore read
                  final snapshot = await FirebaseFirestore.instance
                      .collection('test')
                      .limit(5)
                      .get();
                      
                  print('📦 Found ${snapshot.docs.length} test documents');
                  
                  ScaffoldMessenger.of(context).showSnackBar(
                    SnackBar(
                      content: Text('📦 Found ${snapshot.docs.length} docs'),
                    ),
                  );
                } catch (e) {
                  ScaffoldMessenger.of(context).showSnackBar(
                    SnackBar(content: Text('❌ Error: $e')),
                  );
                }
              },
              child: const Text('Read Test Data'),
            ),
          ],
        ),
      ),
    );
  }
}
````

---

### Step 2: Add Test Route

````dart
import 'package:flutter/material.dart';
import '../../features/landlord/presentation/screens/landlord_home_screen.dart';
import '../../features/shared/presentation/screens/test_firebase_screen.dart'; // Add this

class AppRouter {
  static const String landlordHome = '/landlord';
  static const String testFirebase = '/test-firebase'; // Add this

  static Route<dynamic> generateRoute(RouteSettings settings) {
    switch (settings.name) {
      case landlordHome:
        return MaterialPageRoute(builder: (_) => const LandlordHomeScreen());
      
      case testFirebase: // Add this
        return MaterialPageRoute(builder: (_) => const TestFirebaseScreen());
      
      default:
        return MaterialPageRoute(
          builder: (_) => Scaffold(
            body: Center(
              child: Text('No route defined for ${settings.name}'),
            ),
          ),
        );
    }
  }
}
````

---

### Step 3: Run Test

```bash
cd Residex_app
flutter run
```

**Test Flow:**
1. App launches → Navigate to `/test-firebase`
2. Tap "Test Firestore" button
3. Check Firebase Console → Firestore → `test` collection
4. Should see new document with timestamp

---

## ✅ Verification Checklist

Before proceeding to Phase 2 (CRUD operations):

- [ ] Firebase project created
- [ ] `google-services.json` in `android/app/`
- [ ] Firebase dependencies in `pubspec.yaml`
- [ ] `firebase_options.dart` generated
- [ ] Firestore database created
- [ ] Storage bucket created
- [ ] Authentication enabled (Email + Google)
- [ ] Security rules added
- [ ] Test write succeeded
- [ ] Test read succeeded
- [ ] No Firebase console errors

---

## 🎯 Next Steps (After Firebase Setup)

Once Firebase is confirmed working:

### **Phase 2: Basic CRUD (Week 1)**
1. Landlord creates property
2. Landlord adds unit
3. Tenant signs up
4. Landlord creates lease
5. Tenant creates maintenance ticket

### **Phase 3: AI Integration (Week 2-3)**
1. Lease Generator (uses `leases/` collection)
2. FairFix Auditor (uses `maintenanceTickets/` + Storage)
3. DocuMind RAG (uses documents collection)

---

## 🚨 Common Issues & Solutions

### Issue 1: `google-services.json` not found
**Solution:**
```bash
# Verify file location
ls android/app/google-services.json

# If missing, re-download from Firebase Console
Firebase → Project Settings → Your apps → Download google-services.json
```

### Issue 2: Build fails with Gradle error
**Solution:**
```bash
cd android
./gradlew clean
cd ..
flutter clean
flutter pub get
flutter run
```

### Issue 3: Firebase not initializing
**Solution:**
```dart
// Check main.dart has:
await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);
```

### Issue 4: Firestore permission denied
**Solution:**
- Check security rules in Firebase Console
- Ensure test mode is enabled during development
- Verify user is authenticated before writes

---

## 📚 Resources

- **Firebase Console:** https://console.firebase.google.com/
- **FlutterFire Docs:** https://firebase.flutter.dev/
- **Firestore Data Model Best Practices:** https://firebase.google.com/docs/firestore/data-model

---

**Ready to proceed?** Once you confirm Firebase is working (test write/read succeeds), I'll create:
1. **Property CRUD operations** (Create/Read/Update/Delete)
2. **Lease management system**
3. **Maintenance ticket flow**
4. **Bill upload system**

Then we integrate AI on top of this solid foundation! 🚀

Similar code found with 2 license types